Today it's allitle bit difficult for me, but I'm still trying:
Tuples（タプル(tuple)というのはあんまり聞き慣れない言葉ですが、組 などとも呼ばれ、複数の要素が決まった順番にならんだ値を指します。） are immutable sequences.
Tuple literal are optional parentheses around comma-separated items.
Use a trailing comma(the comma at the end) for single-element.
Tuple unpacking is useful for multiple return values and swapping.
Concatenation(連結(すること)、連鎖、つらなり)
We use str.join()for efficient string concatenation.
Partition(分割)
We use str.partition() for certain simple string parsing operations.
str.format() is a powerful string interpolation(插值) technique.
f-strings are a kind of string literal for performing interpolation. 
